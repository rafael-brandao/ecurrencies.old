
 def  nonBundlorProjects = (groovyProjects + protobufProjects) - bundlorProjects
    
if(nonBundlorProjects.size() > 0) {
    def builder = new StringBuilder()
    nonBundlorProjects.each{builder << "\t${it}\n"}
    logger.error """
WARNING!

The following projects won't have OSGI manifest package scanning , 'template.mf' file is missing :

${builder}
"""
}
bootableClass = 'ecurrencies.Application'
deployFilters += /ecurrencies-application/

dependencies { compile project(':application') }

afterEvaluate {
    jar.enabled = false
    sourcesJar.enabled = false
}